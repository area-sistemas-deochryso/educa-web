# 220 · F1 F5.3 Batch 3 — Services + cleanup + ESLint

- **Plan**: [enforcement-f5.3-re-exports.md](../../plan/enforcement-f5.3-re-exports.md)
- **Fase**: Batch 3 (services + cleanup + ESLint)
- **Modo**: `/execute`
- **Contexto**: Batches 1 y 2 completados y en awaiting-prod.

## Resultado

### Services evaluados

| Service | Consumidores | Decisión |
|---------|-------------|----------|
| `UiMappingService` | 16 archivos, todos `features/intranet/` | → `@intranet-shared` |
| `attendance/*` (8 services + models) | 39+ archivos, todos `features/intranet/` | → `@intranet-shared` |

### Cambios realizados

1. **`git mv`** `shared/services/{ui-mapping,attendance}` → `features/intranet/shared/services/`
2. **Barrel `@intranet-shared/services`**: re-exports agregados
3. **Barrel `@shared`**: removido `export * from './services'` (barrel services queda vacío)
4. **Barrel muerto `@core/services/attendance`**: eliminado (era re-export compat sin consumidores)
5. **62 archivos**: imports actualizados (`@shared/services` → `@intranet-shared/services`, `@core/services` attendance symbols → `@data/models` o `@intranet-shared/services`)
6. **ESLint**: regla `shared-no-features` ya existente cubre prevención (bloquea re-exports inversos)

### Validación

- ✅ Lint (0 errores)
- ✅ Build producción
- ✅ Tests (1 fallo pre-existente: correlation-id-pill)
