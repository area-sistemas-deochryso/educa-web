# 217 — F1 F5.3: Re-exports `@shared` → `@intranet-shared`

> **Plan**: F1 Enforcement de Reglas — F5.3
> **Fecha**: 2026-05-22
> **Scope**: Batch 1/3 — pipes, directives, validators migrados de `@shared` → `@intranet-shared`
> **Chat**: 1 de ~3 (diseño + batch 1)

## Resultado

**24 archivos** editados — imports migrados de `@shared/*` a `@intranet-shared/*` para pipes, directives, validators. Barrels de `@shared` limpiados.

## Entregables

- [x] Plan file: `.claude/plan/enforcement-f5.3-re-exports.md`
- [x] Clasificación shared real vs intranet-only (audit completo)
- [x] Batch 1 ejecutado: pipes (19) + directives (3) + validators (2) = 24 archivos
- [x] Barrels limpiados: `@shared/pipes` (solo full-name), `@shared/directives` (vacío), `@shared/index.ts` (sin directives), `@intranet-shared/index.ts` (+ validators)

## Aprendizajes transferibles

- **`@shared/utils` NO son intranet-only**: `@shared/models` re-exporta desde `@shared/utils` (dependencia circular models↔utils). Mover utils rompe el build. Se revertió.
- **PowerShell `Set-Content` cambia line endings**: `Get-Content -Raw | Set-Content -NoNewline` en pwsh 7 convierte CRLF→LF. Usar `Edit` tool (preserva encoding) en vez de PowerShell para reemplazos en archivos TS.
- **`@app/shared` barrel**: 2 archivos importaban directives vía `@app/shared` (no `@shared/directives`). Grep con ambos patterns.
- **FullNamePipe es nativo de `@shared/pipes`**: 2 archivos mezclan pipes intranet + FullNamePipe. Requieren split de import (no swap simple).

## Trabajo pendiente (batches futuros)

| Batch | Scope | Archivos estimados |
|---|---|---|
| 2 | Components re-exports (7 componentes shimmed en `@shared/components`) | ~20-30 |
| 3 | Services (38 intranet + 1 core) + ESLint rule preventiva + cleanup | ~38+ |

## Validación

- Lint: ✅ 0 errors
- Build: ✅ production OK
- Tests: ✅ 206 suites · 2013 tests · 0 fail
